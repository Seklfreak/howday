import Foundation

/// The five suggested check-in emoji, roughly rough → great. The data model
/// accepts any emoji — the check-in form just offers these.
enum MoodEmoji {
    static let suggestions = ["😢", "😕", "😐", "🙂", "😄"]
}
